services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    volumes:
      - uploads:/app/server/uploads
      - data:/app/server/data
    restart: unless-stopped

volumes:
  uploads:
  data:
